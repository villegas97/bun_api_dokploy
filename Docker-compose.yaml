services:
  pgdb:
    image: postgres:latest
    container_name: postgres_api_bun
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: bun_api_db
    volumes:
      - ./data:/var/lib/postgresql